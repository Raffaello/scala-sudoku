language: scala
scala:
 - 2.12.4

branches:
 only:
  - master

cache:
  directories:
    - '$HOME/.ivy2/cache'
    - '$HOME/.sbt/boot/'

sbt:
  - sbt clean coverage test
  - sbt coverageReport

after_success:
  - bash <(curl -s https://codecov.io/bash)
